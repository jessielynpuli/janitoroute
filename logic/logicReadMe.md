The "Brain" of JanitoRoute. This keeps your heavy math separate from your visual UI screens so the app stays fast.

Here lies
- bfs-engine.ts. This file will contain the pure JavaScript/TypeScript function for Weight-Aware BFS algorithm. It will take the list of dirty bins, crunch the numbers, and output the correct order of locations.