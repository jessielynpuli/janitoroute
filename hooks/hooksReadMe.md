What it is: Advanced React features that manage state or handle specific mobile triggers globally.

What goes inside: Files like useColorScheme.ts (for handling dark/light mode automatically). Later, you can build a custom hook here to handle real-time database subscriptions.