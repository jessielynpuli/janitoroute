Expo Router looks at this folder, and automatically turns files into pages (File-Based routing). For Layout implementation

Here lies
- _layout.tsx (skeleton of the app, defines bottom nav tab bar)
- index.tsx (very first screen that opens)
- tabs/ (holds screens for sub-pages like explore.tsx -> turn into janitors path list)

